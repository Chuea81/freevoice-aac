name: Security Audit

on:
  schedule:
    - cron: '0 9 * * 1'  # Every Monday at 9am UTC
  push:
    paths:
      - 'package.json'
      - 'package-lock.json'

jobs:
  audit:
    name: npm audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - name: Run security audit (production deps only)
        # The previously-allowlisted transitive vulns (protobufjs critical,
        # @xmldom/xmldom high) are now patched via `overrides` in package.json,
        # so this is back to a plain high/critical gate with no exceptions.
        run: npm audit --audit-level=high --omit=dev
      - name: Check ARASAAC license compliance
        run: |
          echo "Checking for prohibited symbol sources..."
          if npm ls | grep -iE "pcs|symbolstix|widgit|mayer-johnson"; then
            echo "ERROR: Prohibited symbol library detected in dependencies"
            exit 1
          fi
          echo "✅ No prohibited symbol sources detected"
